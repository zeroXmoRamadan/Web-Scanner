# ──────────────────────────────────────────────
# Core runtime dependencies
# ──────────────────────────────────────────────
httpx>=0.27.0
typer>=0.12.0
pyyaml>=6.0
jinja2>=3.1.0
python-nmap>=0.7.1
rich>=13.7.0
wappalyzer>=2.0.0
beautifulsoup4>=4.12.0

# ──────────────────────────────────────────────
# Testing dependencies
# ──────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0

# ──────────────────────────────────────────────
# Optional: --deep-fingerprint mode
#
#   The default fingerprint mode (scan_type="fast") uses HTTP-only analysis
#   and does NOT require Chromium or Playwright.
#
#   To enable --deep-fingerprint (scan_type="full"), install Chromium:
#     python -m playwright install chromium
# ──────────────────────────────────────────────
